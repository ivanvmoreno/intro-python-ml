---
title: "Introducción a ML con Python 🐍"
subtitle: "02_01 - `pandas`"
author: "Iván Moreno"
lang: "es"
format: 
  revealjs:
    embed-resources: true
    theme: [dark]
    output-location: fragment
execute:
    echo: true
engine: jupyter
editor_options: 
  markdown: 
    wrap: 99
---

# Tipos de datos en DS

## Datos estructurados – Dataframes

* Tablas de datos heterogéneos
* Comúnmente referidos como *tablas*
* Cada fila es una observación o *sample*
* Cada columna es una variable o *feature*

. . .

* Todas las observaciones deben tener el mismo número de variables, aunque pueden tener valores nulos o *missing values*
* Los valores de las variables pueden ser de diferentes tipos, aunque todos los valores de una misma variable deben ser del mismo tipo

## Variables categóricas

* Variables que toman valores de un conjunto finito y discreto de valores
* Pueden ser ordinales (ordenadas, e.g. `low`, `medium`, `high`) o nominales (no ordenadas, e.g. `red`, `green`, `blue`)
* Se pueden representar como cadenas de texto o como enteros
* En el caso de representarlas como enteros, se denominan *variables dummy* o *variables indicadoras*

## Almacenado de datos estructurados

* Los dataframes se pueden almacenar en diferentes formatos
* Los más comunes son CSV, SQL, Apache Arrow, etc.

## Datos no estructurados

## JSON

* Formato de texto para el intercambio de datos (APIs, etc.)
* Basado en la sintaxis de JavaScript
* Formado por pares clave-valor

## XML

* Similar a JSON, pero con una sintaxis más compleja, basada en etiquetas
* Espacialmente menos eficiente que JSON (etiquetas)

## Imágenes

* Tensores de 3 dimensiones (ancho, alto, canales)
* Cada elemento del tensor es un valor de intensidad de un canal de color
* Los valores de intensidad pueden ser enteros o reales
* Dependiendo del tipo de imagen, los canales pueden ser RGB, HSV, etc.
* Múltiples formatos de almacenamiento (PNG, JPEG, etc.), dependiendo del caso de uso

## Más alla

* Audio – Tensores de 1 dimensión (muestras)
* Texto (no estructurado) – NLP

# `pandas`

## ¿Qué es `pandas`?

* Librería de Python para trabajar de manera eficiente con datos heterogéneos (tablas, series temporales, etc.)
* Forma parte de `SciPy`, junto con `numpy`, `matplotlib` y `scikit-learn`
* Permite leer y escribir datos en diferentes formatos (CSV, Excel, SQL, etc.)
* Se basa en las colecciones `Series` y `DataFrame`

## `Series`

* Estructuras unidimensionales similares a los `ndarray` de `numpy`
* Cada elemento tiene un índice asociado
* Se pueden crear a partir de listas, diccionarios, `ndarray`, etc.

```python
s = pd.Series([1, 2, 3, 4]) # índices por defecto
s = pd.Series([1, 2, 3, 4], index=['a', 'b', 'c', 'd']) # índices semánticos
```

## `DataFrame`

* Podemos entender un `DataFrame` como una colección de `Series` concatenadas verticalmente, compartiendo un listado de índices común
* Se pueden crear a partir de diccionarios, listas de listas, `ndarray`, etc.

```python
df = pd.DataFrame({'a': [1, 2, 3], 'b': [4, 5, 6]})
df['a'] # devuelve una serie
```

## Tipos de índices

* Los índices pueden ser __posicionales__ o __semánticos__
* Los índices posicionales son enteros del 0 al `len(s) - 1`
* Los índices semánticos son cadenas de texto, enteros, etc.

```python
s = pd.Series([1, 2, 3, 4], index=['a', 'b', 'c', 'd'])
s[0] # índice posicional
s['a'] # índice semántico
```

## Indexado en `Series` y `DataFrame`

* `Series` permiten indexado como los `ndarray` (por índice posicional (e.g. `s[0]`) o semántico (e.g. `s['a']`))
    * Por defecto, los índices semánticos son los enteros del 0 al `len(s) - 1`
* `DataFrame`, además, permiten indexado por columna

## Indexado en `Series` y `DataFrame`

* `pandas` siempre interpreta enteros como posiciones, no como índices semánticos (e.g. `df[0]` devuelve la primera columna, no la columna con índice `0`)
    * Para evitar confusiones, se puede usar `loc` o `iloc`
    * `loc` permite indexado y slicing por índice semántico
    * `iloc` permite indexado y slicing por posición
* En el caso de `DataFrame`, tanto `loc` como `iloc` permiten indexado y slicing por filas y columnas, especificando el eje con el argumento `axis`

## Indexado – Ejemplos

```python
s = pd.Series([1, 2, 3, 4], index=['a', 'b', 'c', 'd'])
s[0]
s['a']
```

```python
df = pd.DataFrame({'a': [1, 2, 3], 'b': [4, 5, 6]})
df.loc[0, 'a'] # devuelve el elemento de la primera fila y la columna 'a'
df.iloc[0, 0] # equivalente
```

## Máscaras booleanas

* Una máscara booleana es una expresión que devuelve un valor booleano para cada elemento de una colección
* Se pueden combinar máscaras booleanas con operadores lógicos (`&`, `|`, `~`)
* Se pueden usar máscaras booleanas para filtrar datos

```python
df = pd.DataFrame({'a': [1, 2, 3], 'b': [4, 5, 6]})
df['a'] > 1
```

## Búsqueda de datos – `isin`

* `<objeto>` puede ser una `Series` o un `DataFrame`
* `<objeto>.isin(<lista>)`: devuelve una máscara booleana con los elementos que están en la lista

```python
df = pd.DataFrame({'a': [1, 2, 3], 'b': [4, 5, 6]})
df.loc[df['a'].isin([1, 2])]
```

## Búsqueda de datos – `loc`

* `<objeto>.loc[<máscara>]`: devuelve los elementos que cumplen la máscara

```python
df = pd.DataFrame({'a': [1, 2, 3], 'b': [4, 5, 6]})
df.loc[df['a'] > 1] # devuelve las filas cuyo valor en la columna 'a' es mayor que 1
```

## Búsqueda de datos – `in`
* `<objeto> in <lista>.index`: booleano que indica si el objeto está en la lista de índices semánticos
* `<objeto> in <lista>.columns`: booleano que indica si el objeto está en la lista de columnas (solo para `DataFrame`)

```python
df = pd.DataFrame({'a': [1, 2, 3], 'b': [4, 5, 6]})
'a' in df.columns
```

## Slicing

* `Series` permiten slicing como los `ndarray` (por índice posicional o semántico)
* El slicing por índice posicional excluye el extremo superior (al igual que en `numpy`)
* El slicing por índice semántico incluye ambos extremos
* `DataFrame` permiten slicing por filas y columnas, especificando el eje con el argumento `axis`

```python
s = pd.Series([1, 2, 3, 4], index=['a', 'b', 'c', 'd'])
s[0:2]
s['a':'c']
```

```python
df = pd.DataFrame({'a': [1, 2, 3], 'b': [4, 5, 6]})
df.loc[0:1, 'a':'b'] # devuelve las dos primeras filas y las columnas 'a' y 'b'
```

## Filtrado de datos

* `loc`e `iloc` son útiles para filtrar filas y columnas simultáneamente, y trabajar con un subconjunto de los datos
* También es posible indexado condicional por máscaras booleanas, que se pueden generar con operadores lógicos

```python
df = pd.DataFrame({'a': [1, 2, 3], 'b': [4, 5, 6]})
df.loc[df['a'] > 1, 'b'] # devuelve los valores de la columna 'b' cuyo valor en la columna 'a' es mayor que 1
```

## Añadir y eliminar columnas

* Se pueden añadir columnas a un `DataFrame` simplemente asignando una serie a una nueva columna
* Se pueden eliminar columnas con el método `drop`, indicando el nombre de la columna y el eje con el argumento `axis`

```python
df = pd.DataFrame({'a': [1, 2, 3], 'b': [4, 5, 6]})
df['c'] = [7, 8, 9]
df.drop('c', axis=1)
```

## Ordenado de datos

* `Series` y `DataFrame` tienen el método `sort_values`, que permite ordenar los datos por los valores de una columna o serie
* El método `sort_index`, que permite ordenar los datos por los índices semánticos o posicionales
* `rank` retorna el rango de cada elemento de la serie o columna, ordenados por los valores de la serie o columna
* Todas estas operaciones permiten especificar orden ascendente o descendente con el argumento `ascending`

```python
df = pd.DataFrame({'a': [1, 2, 3], 'b': [4, 5, 6]})
df.sort_values('a', ascending=False)
```

## Operaciones aritméticas

* Las operaciones se realizan elemento a elemento
* Aquellos casos en los que no hay un valor para una combinación de índices, se rellenan con `NaN`
* `pandas` permite especificar un valor por defecto para los valores `NaN` con el argumento `fill_value`

| Método | Ejemplo | Expresión Equivalente |
|-----------|-------------|-----------------------|
| `add` | `df1.add(df2)` | `df1 + df2` |
| `sub` | `df1.sub(df2)` | `df1 - df2` |
| `mul` | `df1.mul(df2)` | `df1 * df2` |
| `div` | `df1.div(df2)` | `df1 / df2` |

## `apply`

* Permite aplicar una función a cada elemento de una serie o columna
* La función puede ser una función lambda o una función definida por el usuario
* `apply` también se puede usar para aplicar una función a cada fila o columna de un `DataFrame`, especificando el eje con el argumento `axis`

```python
df = pd.DataFrame({'a': [1, 2, 3], 'b': [4, 5, 6]})
df.apply(lambda x: x ** 2)
```

## Estadística descriptiva

* `describe` calcula estadística descriptiva básica (media, desviación estándar, cuartiles, etc.)

| Método | Descripción |
|--------|-------------|
| `count` | Número de elementos no nulos |
| `describe` | Estadística descriptiva básica |
| `min`, `max` | Mínimo y máximo |
| `argmin`, `argmax` | Índice del mínimo y máximo |
| `idxmin`, `idxmax` | Índice semántico del mínimo y máximo |
| `sum` | Suma |
| `mean` | Media |
| `median` | Mediana |
| `std`, `var` | Desviación estándar y varianza |
| `skew` | Asimetría |
| `quantile` | Cuantiles |
| `cumsum` | Suma acumulada |
| `diff` | Diferencia entre elementos consecutivos |

## Varianza y desviación estándar

## Varianza

* `var` calcula la varianza
    * $\sigma^2 = \frac{1}{N} \sum_{i=1}^N (x_i - \mu)^2$
    * Es una medida de dispersión que indica cuánto se alejan los valores de la media (esperanza)
    * Es la media aritmética de las desviaciones al cuadrado de cada elemento con respecto a la media
        * Se elevan al cuadrado para evitar que las desviaciones negativas se cancelen con las positivas
    * Expresa el grado de cambio de una variable respecto a su media en el tiempo

```python
df = pd.DataFrame({'a': [1, 2, 3], 'b': [4, 5, 6]})
df.var() # varianza
```

## Desviación estándar

* `std` calcula la desviación estándar
    * $\sigma = \sqrt{\frac{1}{N} \sum_{i=1}^N (x_i - \mu)^2}$
    * Es la raíz cuadrada de la varianza
    * Expresa la dispersión (fluctuación / volatilidad) en las mismas unidades que los datos

```python
df = pd.DataFrame({'a': [1, 2, 3], 'b': [4, 5, 6]})
df.std() # desviación estándar
```

## Covarianza y correlación

## Covarianza

* `cov` calcula la covarianza entre columnas o series
    * $cov(X,Y) = E[(X - E[X])(Y - E[Y])]$
    * Es la media aritmética de los productos de las desviaciones de cada variable con respecto a su media
        * Intuitivamente, mide el grado de variación conjunta de dos variables aleatorias respecto a sus medias
    * Una covarianza positiva indica una relación lineal positiva, mientras que una covarianza negativa indica una relación lineal negativa

```python
df = pd.DataFrame({'a': [1, 2, 3], 'b': [4, 5, 6]})
df.cov() # covarianza
```

## Correlación

* `corr` calcula la correlación entre columnas o series
    * $\rho_{X,Y} = \frac{cov(X,Y)}{\sigma_X \sigma_Y}$
    * Se define como la covarianza estandarizada, es decir, la covarianza dividida por el producto de las desviaciones estándar de las dos variables
    * Nos da información sobre la fuerza y la dirección de la relación lineal entre dos variables aleatorias, al contrario que la covarianza, que solo nos da información sobre la dirección

```python
df = pd.DataFrame({'a': [1, 2, 3], 'b': [4, 5, 6]})
df.corr() # correlación
```
